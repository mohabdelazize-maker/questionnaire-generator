// دالة لتحويل LaTeX إلى عرض HTML\nexport const renderLatexToHTML = (latex: string): string => {\n  return `\\\\(${latex}\\\\)`\n}\n\n// دالة لتحويل النص مع الصيغ الرياضية\nexport const processTextWithMath = (text: string): { plain: string; latex: string } => {\n  const latexRegex = /\\$\\$([^$]+)\\$\\$/g\n  const plain = text.replace(latexRegex, '$1')\n  return {\n    plain,\n    latex: text,\n  }\n}\n\n// دالة للتحقق من صحة صيغة LaTeX\nexport const isValidLatex = (latex: string): boolean => {\n  try {\n    const braces = latex.match(/[{}]/g) || []\n    let balance = 0\n    for (const brace of braces) {\n      balance += brace === '{' ? 1 : -1\n      if (balance < 0) return false\n    }\n    return balance === 0\n  } catch {\n    return false\n  }\n}\n\n// دالة لتحسين صيغة LaTeX\nexport const improveLatex = (latex: string): string => {\n  let improved = latex\n  improved = improved.replace(/(\\w)(\\+|\\-|=)(\\w)/g, '$1 $2 $3')\n  improved = improved.replace(/frac\\s*\\{/g, 'frac{')\n  return improved\n}\n\n// مجموعة من الصيغ الشائعة\nexport const commonFormulas = {\n  quadratic: 'x = \\\\frac{-b \\\\pm \\\\sqrt{b^2-4ac}}{2a}',\n  pythagoras: 'a^2 + b^2 = c^2',\n  distance: 'd = \\\\sqrt{(x_2-x_1)^2 + (y_2-y_1)^2}',\n  areaCircle: 'A = \\\\pi r^2',\n  volumeSphere: 'V = \\\\frac{4}{3}\\\\pi r^3',\n  sine: '\\\\sin^2(\\\\theta) + \\\\cos^2(\\\\theta) = 1',\n  limit: '\\\\lim_{x \\\\to a} f(x) = L',\n  integral: '\\\\int_a^b f(x)dx = F(b) - F(a)',\n  derivative: '\\\\frac{d}{dx}[f(x)] = \\\\lim_{h \\\\to 0} \\\\frac{f(x+h)-f(x)}{h}',\n}\n\nexport const testLatexRendering = (latex: string): void => {\n  console.log('Testing LaTeX:', latex)\n  console.log('Is valid:', isValidLatex(latex))\n  console.log('Improved:', improveLatex(latex))\n}\n